submit = list(map(int,[input() for _ in range(28)]))
students = {}

for ()
students[num] = 

for num in range(30):
    isSubmit = False
    for student in submit:
        if num + 1 == student:
            isSubmit = True
    if isSubmit == False:
        print(num + 1)
    isSubmit = False